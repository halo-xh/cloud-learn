server:
  port: 13000

management:
  endpoints:
    web:
      exposure:
        include: refresh

spring:
  cloud:
    bus:
      trace:
        enabled: true
      enabled: true

# 注册到eureka
#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:7000/eureka/